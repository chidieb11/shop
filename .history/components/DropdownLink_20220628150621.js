import { resolveHref } from "next/dist/shared/lib/router/router";
import React from "react";

const DropdownLink = (props) => {
  return <div>let {resolveHref,chi}</div>;
};

export default DropdownLink;
